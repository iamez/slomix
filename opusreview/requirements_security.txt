# Security Requirements for SLOMIX Bot
# Install with: pip install -r requirements_security.txt

# Existing dependencies (update versions for security)
discord.py>=2.3.0
asyncpg>=0.29.0
paramiko>=3.4.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# New security dependencies
cryptography>=41.0.7       # For secure credential encryption
keyring>=24.3.0            # For system credential storage (optional)
validators>=0.22.0         # For input validation
bandit>=1.7.5             # For security scanning
safety>=3.0.0             # For dependency vulnerability scanning

# Optional monitoring
sentry-sdk>=1.40.0        # For error tracking (optional)
prometheus-client>=0.19.0  # For metrics (optional)
