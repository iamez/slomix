# Known disagreements between the hand-written SPA types
# (website/frontend/src/app/lib/types.ts) and the generated OpenAPI schema,
# as reported by scripts/check_manual_types_against_openapi.py --json.
#
# RECOMPUTED by tests/unit/test_manual_types_do_not_drift_further.py. This file
# is the record; the test fails in BOTH directions, so a fixed line has to be
# deleted in the commit that fixes it.
#
# declares non-null, and a field the API always sends that TS marks optional.
# Seeded 2026-09-01 with six entries, ALL SIX CLEARED THE SAME DAY by
# `7e381f45` on #861 — the parallel session fixed them at the source once this
# ratchet gave the checker a caller. The file is deliberately left in place
# EMPTY: the guard is the point, not the list, and a next disagreement should
# land as a failing test rather than as a new file nobody remembers to add.
#
# ⭐ The two-way protocol worked and is worth keeping: whoever merges SECOND
# clears the lines. CI runs a PR against a MERGE with main, so #860 saw the
# fix from #861 before its own branch did, and failed with 'stale' — which is
# exactly the signal, arriving from the direction nobody was watching.
