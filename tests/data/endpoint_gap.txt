# H1 endpoint ratchet (docs/design/09 §H1) — paths the legacy frontend calls
# (plus endpoint_required_extra.txt) that the NEW frontend does not cover yet.
# Every build phase deletes lines; the goal is an EMPTY file by switchover.
# Before deleting a line: verify against live /openapi.json AND call it once
# (docs/design/07 §C.1). Seeded 2026-08-24 from the extractors themselves.
#
# 2026-08-29: 64 -> 120. Not a regression — a correction. The extractor read
# all of website/frontend/src, so the OLDER React tree (src/pages) counted as
# migration progress and 56 paths it alone calls looked covered. The scan
# root is src/app now; these 56 were always missing.
#
# 2026-08-30, /api/stats/sessions: this line will NOT close by the new app
# adopting the path. Owner's decision that day was that /api/sessions is the
# source for session listings, and it now carries everything its sibling had
# (duration_seconds, start_time, end_time, player_names, total_deaths, search)
# with both endpoints agreeing on all of them. The line stays because the
# ratchet measures what legacy calls and the new app does not, which is still
# true and should stay visible; it closes when legacy sessions.js/sessions2.js
# are retired, not before. Do not delete it to make the number smaller — the
# test recomputes the set and would fail anyway, which is the ratchet working.
#
# ---------------------------------------------------------------------------
# 2026-09-06 — TRIAGE BY DATA, not by line count.
#
# The ratchet counts PATHS. It cannot see that these 13 are wildly unequal in
# what they would actually show a visitor. Measured against the live dev
# database that evening, so the next person picks up the ones that pay:
#
#   /api/rounds/{}/awards                  round_awards          27269 rows
#       ^ CLOSED the same evening — it was the only line the data justified.
#         Closing it also had to fix a disagreement: the session aggregate
#         over this table drops bots and the per-round endpoint did not, so
#         2120 bot rows across 83 rounds were visible in one view and hidden
#         in the other. 97% of them sit in rounds with no human award at all.
#   /api/greatshot/{}/highlights/render    greatshot_highlights    622 rows
#                                          greatshot_renders         1 row
#   /api/greatshot/{}/crossref             greatshot_demos          23 rows
#   /api/uploads/{}/download               uploads                   8 rows
#
# ⭐ ROW COUNTS ALONE PICKED WRONG. Adding the dates changed the order:
#
#   greatshot_demos       23 rows   2026-02-08 .. 2026-09-02   alive
#   greatshot_highlights 622 rows   2026-02-08 .. 2026-09-02   alive
#   greatshot_renders      1 row    2026-02-15 .. 2026-02-15   used ONCE, 7
#                                                              months ago
#
# So /highlights/render sits on 622 rows and looks like the bigger prize,
# while the feature it drives has been used exactly once since February. Its
# neighbour /crossref has a quarter of the rows and is still in use. A count
# says how much data exists; only a date says whether anyone wants it.
#
# ⚠️ /crossref is also per-user (`_require_user`, `WHERE user_id = $2`), so
# those 23 demos are split across accounts — each visitor sees a slice.
#   /api/players/{}/awards                 season_awards             2 rows
#   /api/bets                              parimutuel_bets           0 rows
#
# The rest (/card, /vs-stats, /graphs, /stats/player/{}/form and /rounds,
# /rounds/{}/player/{}/details, /stats/sessions) are composites over
# player_comprehensive_stats, where the data is not in question.
#
# ⛔ Two of these would ship an empty page. `/api/bets` has no rows at all and
# is an owner decision anyway; `/api/players/{}/awards` has 2 rows across 2
# players and 1 season. Building UI for them closes a ratchet line and shows a
# visitor nothing — the repo already has a name for that shape (a mechanism
# without a consumer), and the ratchet is exactly the kind of number that
# invites it, because 0 rows and 27269 rows cost the same to close.
#
# ⚠️ This is a note about VALUE, not about correctness. Every line here is
# genuinely uncovered; none may be deleted for being small. Backend handlers
# already exist for 12 of the 13 (checked against the router decorators), so
# the remaining work is frontend, not API.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# 2026-09-07 — ORDER OF WORK for the 12 that remain, and what each costs.
#
# Row counts separate the first group; for the composites they separate
# nothing, since all of them read player_comprehensive_stats. What separates
# THOSE is whether the port can copy legacy or has to fix it on the way.
#
# DEAD — do not build UI until the data exists:
#   /api/bets                            0 rows, and an owner decision anyway
#   /api/players/{}/awards               2 rows, 2 players, 1 season
#   /api/greatshot/{}/highlights/render  greatshot_renders: 1 row, 2026-02-15
#
# ALIVE, small, per-user:
#   /api/greatshot/{}/crossref           23 demos to 2026-09-02, but behind
#                                        `_require_user` + WHERE user_id, so
#                                        each visitor sees only their own
#   /api/uploads/{}/download             8 rows
#
# ⛔ CANNOT BE COPIED AS-IS — the legacy call carries a bug:
#   /api/sessions/{}/graphs              The path parameter is a DATE. The
#     handler accepts an optional `gaming_session_id` query param and prefers
#     it, but 2 of the 3 legacy call sites omit it (diagnostics.js:296,
#     sessions.js:2025; only sessions.js:881 passes one). CLAUDE.md forbids
#     date-based session queries for exactly this reason: measured 2026-09-07,
#     13 of 176 days (7.4%) hold more than one session, up to 4. On those days
#     the two bare calls MERGE sessions into one graph. Porting this means
#     passing gaming_session_id always — not reproducing the legacy call.
#
# COMPOSITES over player_comprehensive_stats — data is not the question:
#   /api/players/{}/card                 rating + 90-day form + percentiles
#   #   /api/rounds/{}/vs-stats              ⚠️ note /player/{guid}/vs-stats is a
#                                        DIFFERENT endpoint (records_player.py)
#   /api/stats/player/{}/form
#   /api/stats/player/{}/rounds
#
# OWNER DECISION, closes with legacy retirement:
#   /api/stats/sessions                  (see the 2026-08-30 note above)#
# 2026-09-08, 9 -> 11: the legacy extractor now reads a call that ENDS in an
# interpolated segment as a one-parameter SHAPE (`/api/stats/matches/{}`), not
# as the list prefix — the list call the new app makes had been clearing the
# unbuilt detail (same class as the 2026-09-05 mid-path fix, from the other
# end). The two shapes that surfaced:
#   /api/stats/matches/{}   matches.js:634 + session-detail.js — the match
#                           modal (team1/team2/is_winner per team, 20 fields).
#                           Closes with a match expansion on RoundsTable.
#   /api/sessions/{}        sessions.js:1617 + session-detail.js:359 — a session
#                           BY DATE. The new app keys sessions by
#                           gaming_session_id (/api/stats/session/{gsid}/*, the
#                           CLAUDE.md direction), which carries everything this
#                           returned except start_time / end_time /
#                           gaming_session_ids. Closes by adding those to
#                           /basics and retiring the date-keyed call, never by
#                           porting the date path (a day with two sessions
#                           merges them).
# A third shape, /api/uploads/resumable/{}, was a phantom: the new app drives
# it (HEAD/PATCH/DELETE in lib/uploads/resumable.ts) through a bare spec
# literal the new-tree extractor's charset cut at `{`; fixed in the same
# change, so it never appeared here.
# ---------------------------------------------------------------------------
#
# 2026-09-02,
#
# 2026-09-06: 3 -> 15. A CORRECTION, the second of its kind (see 64 -> 120
# above), and it is about the RATCHET, not about new work appearing.
#
# The legacy extractor captured "up to the first `?`, quote or `${`". So
# `wrapped.js`'s `${API_BASE}/players/${guid}/wrapped` was read as the prefix
# `/api/players` — and `_covered()` treats a truncated prefix as covered the
# moment the new app calls anything deeper. It does call
# `/api/players/{identifier}/profile`, so a DIFFERENT, unbuilt operation
# cleared it. Measured: 29 legacy calls carry an interpolation with at least
# one segment behind it. The extractor now reads THROUGH interpolations and
# writes each as `{}`, so a call is required as a SHAPE only a real call to
# that shape can clear (test_route_contract._FE_FULL_PATH_RE).
#
# Every line below was verified twice: it resolves to a live path in
# docs/api/openapi.json, and `src/app` contains no call to it.
#
# TRIAGE, by CALLING the endpoints rather than reading their names — three
# lines look superseded and are not:
#   * players/{}/awards -> {status, guid, awards}: ENGRAVED SEASON AWARDS on
#     the legacy History tab, not the "milestones" the profile already shows.
#   * stats/player/{}/rounds -> {rounds:[{label,date,dpm}], avg_dpm}: a DPM
#     series feeding a canvas sparkline. The profile's "last rounds" TABLE
#     comes from /api/player/{name}/matches. Same word, different data.
#   * players/{}/card -> {rating, archetype, percentiles, sparkline_dpm,
#     badges, career}: the hover card. Its percentiles are NOT the ET rating
#     components' — measured on one player, card dpm 57 / survival 64 /
#     revives 79 against components 58.9 / 83.9 / 44.6. Different pool,
#     different window.
#
# ⛔⛔ TWO LINES THAT SHOULD NOT BE MIGRATED AS THEY STAND:
#   * rounds/{}/vs-stats — the handler (records_matches.py:491) selects from
#     round_vs_stats with NO GROUP BY and drops subject_guid. That table holds
#     one row per duel pairing, so the response is a raw dump. Measured on
#     round 11425: 18 rows for 6 players, `vid` three times. Legacy's own "VS
#     Stats" table already shows the same player several times. Fix the
#     handler (GROUP BY player_guid, subject_guid) or delete the line when
#     matches.js retires — porting it verbatim moves the bug into the new app.
#   * greatshot/{}/highlights/render — a POST that queues a render job. The
#     renderer is NOT configured here: no ffmpeg, no GREATSHOT_RENDER_COMMAND,
#     no .env. Hard evidence: greatshot_renders holds exactly ONE row ever
#     (2026-02-15), status 'failed', with that configuration error — against
#     622 highlights. The button would always produce queued -> failed.
#
# ⚠️ uploads/{}/download is FUNCTIONALLY COVERED already: the server hands the
# page `download_url` (uploads.py:736) and UploadsPage.tsx:325 renders it, so
# the path never appears as a literal in `src/app` and the extractor cannot
# see it. The line stays because the ratchet measures literals, like
# /api/stats/sessions above. What IS missing there is smaller and separate: no
# download button on the list card, and no inline player (`is_playable` /
# `poster_url` are unused).
#
# ⚠️ sessions/{}/graphs counts `round_number IN (1,2)` with its own status
# filter, while the Stats 2.0 endpoints use `counts_toward_totals`. Any panel
# built on it must say which gate it used, or the site will show two different
# kill totals for one evening.
#
# 2026-09-08, /api/rounds/{}/player/{}/details DELETED: built as the inline
# breakdown under a clicked player row of the session Rounds tab
# (RoundPlayerDetailsPanel). Verified per docs/design/07 §C.1 — 200 on the
# live server for round 11430, response committed as a fixture. ⛔ The legacy
# matches.js:970 read `combat.useful_kills` and `w.weapon_name`; the recording
# has `support.useful_kills` and `w.name` (a WS_ token, labelled by
# lib/weapons.ts). The type was written from the recording, not from legacy.
#
# 2026-09-06, /api/players/{}/memory-card DELETED: built as a profile section.
# Verified per docs/design/07 §C.1 — 200 on the live server, response
# committed as a fixture. ⚠️ It is NOT the wrapped card of the same two names:
# wrapped is per season (signature = most played + win %, best round = best
# DPM), this is career (signature = biggest lift over the player own average,
# best round = most kills). Measured on one player before building.
#
# 2026-09-06, /api/player/{}/vs-stats DELETED: built as the sixth instrument
# in PlayerDrilldown, session-scoped. Rivalries already answers "who is my
# nemesis" ALL-TIME; this answers "who did I feed tonight", and the numbers
# differ by an order of magnitude (51 kills against the top prey this session,
# 938 all-time on the same pair). ⛔ The call must send scope AND session_id:
# the handler branch is `elif scope == "session" and session_id`
# (records_player.py:36), so a scope without its id silently returns all-time.
# Pinned by a test that reads the request URL.
/api/bets
/api/greatshot/{}/highlights/render
/api/players/{}/awards
/api/players/{}/card
/api/rounds/{}/vs-stats
/api/sessions/{}
/api/sessions/{}/graphs
/api/stats/matches/{}
/api/stats/sessions
/api/uploads/{}/download
