[pytest]
# Only run tests in the tests/ directory by default
testpaths = tests

# Ignore large backup/publish directories and venv
norecursedirs = .venv backups publish_temp publish_clean publish_temp publish_temp\publish_temp test_suite tmp

# Ensure project root is on PYTHONPATH
pythonpath = .

# Add quiet output by default
addopts = -q
