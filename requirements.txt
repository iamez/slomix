# Runtime dependencies (pinned for reproducible builds)
discord.py==2.6.4
asyncpg==0.31.0
# Sync driver used ONLY by standalone ops/backfill scripts in scripts/
# (backfill_orphan_r2.py, backfill_rounds_is_valid.py, backtest_krogt_perlife.py, ...).
# The bot and website both use asyncpg; without this pin those prod ops scripts
# fail on a fresh venv and have to fall back to hand-written static SQL.
psycopg2-binary==2.9.11
aiosqlite==0.22.1
python-dotenv==1.2.2
pytz==2025.2
itsdangerous==2.2.0
fastapi==0.133.1
uvicorn[standard]==0.41.0
httpx==0.28.1
python-multipart==0.0.31
aiofiles==25.1.0
paramiko==4.0.0
scp==0.15.0
websockets==16.0
Pillow==12.2.0
matplotlib==3.10.8
watchdog==6.0.0
trueskill==0.4.5
redis==7.2.1
prometheus-client==0.24.1
prometheus-fastapi-instrumentator==7.1.0
slowapi==0.1.9
