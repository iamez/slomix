# Slomix Project Audit — 2026-05-08

Mandelbrot RCA-style audit across three layers. Initiated overnight while
user was sleeping; bundle of fixes opened in PR for morning review.

## Methodology

3 parallel Explore agents, each covering a distinct layer:

- **Layer A — Vision delivery**: does the "stories WITH numbers" vision
  actually fire on the live website? Endpoints + frontend rendering +
  data populated end-to-end.
- **Layer B — Schema drift / INSERT coverage**: find columns added by
  recent migrations that no INSERT path populates (mimicking the
  `*_guid_canonical` regression already fixed in commit cdb7f51).
- **Layer C — Recent regressions + code health**: PRs #186/#188/#190/#192
  cleanliness, lint state, silent failures, dead code, doc freshness.

Each agent was instructed to prioritize **CRITICAL** (act tonight) /
**IMPORTANT** (act tonight) / **NIT** (defer), and surface the 3 highest-
value findings rather than 30 small ones.

## Findings & Decisions

### Layer A — Vision delivery

| Severity | Finding | Decision |
|----------|---------|----------|
| IMPORTANT | `/api/skill/composite` endpoint computes TIR/CI/KPI/SDS/CP per player but **Story page never calls it** — the "numbers" half of "stories WITH numbers" is missing UI | **Fixed** — new `useStoryComposite` hook + `CompositeStatsPanel` component, wired below `InvisibleValuePanel` on Story page |
| NIT | Discord bot lacks ET Rating / storytelling commands | Defer (web surfaces it; Discord gap is non-blocking) |
| NIT | KIS `is_objective_area` and `distance_multiplier` hardcoded | Defer (placeholder, doesn't block scoring) |

**Vision scorecard**: endpoints exist (✅), data populated (✅:
`storytelling_kill_impact`=13,995 rows, 0 NULLs; `player_skill_ratings`=48
players), most frontend panels render (✅: gravity, space, enabler,
lurker, synergy, win-contribution, box-score, momentum, narratives), but
the **composite stats fingerprint was a hidden completion gap**. Now
visible.

### Layer B — Schema drift

| Severity | Finding | Decision |
|----------|---------|----------|
| ⚠️ NEEDS USER REVIEW | `rounds.round_canonical_id` is 82.3% NULL (1900/2309 rows) on local samba DB. Migration 049 added the column; backfill tool exists at `tools/backfill_round_canonical_id.py`. Forward-compat is already correct (dual-write at Lua webhook arrival in `ultimate_bot.py:1487-1516`); this is historical data. | **Backfill tool fixed** — `tools/backfill_round_canonical_id.py` now reads `POSTGRES_*` canonical env names (was `DB_*` only, didn't work with current `.env`). Running the backfill itself is left for user — it's a data write and the user owns when to do that. After it's run, migration 050 (UNIQUE constraint) becomes safe to apply. |
| OK | Pre-linker proximity NULL `round_id` (0.4-0.5%) | Historical, not forward — no action |
| NIT | `tools/schema_postgresql.sql` missing `round_canonical_id` definition | Defer — fresh installs run all migrations in order anyway |
| OK | `*_guid_canonical` columns: 0 NULLs across all 5 tables, fix from cdb7f51 working | No action |

### Layer C — Recent regressions + code health

| Severity | Finding | Decision |
|----------|---------|----------|
| IMPORTANT | `SESSION_DETAIL_TABS = ['summary','players','teamplay','charts']` defined in **both** `route-registry.js` and `session-detail.js` — adding/removing a tab in one file silently breaks the other | **Fixed** — exported from `route-registry.js`, imported in `session-detail.js`. Single source of truth. |
| NIT | CLAUDE.md claims `Version: 1.5.0` but `package.json` is `1.13.1` (8 minor versions stale) | **Fixed** — bumped 3 version refs to 1.13.1 |
| FALSE POSITIVE (skipped) | "PR #186 incomplete `bg-brand-blue` → `bg-cyan-500/15` migration: 11 remaining instances" | The remaining `bg-brand-blue` instances are **deliberate team A color** (paired with `text-brand-rose` for team B). PR #186's scope was explicitly only the tab nav. Not a regression. |
| OK | pytest collection: agent claimed `archive/diagnostics/test_bot_startup.py` breaks collection; actual run shows 0 errors | No action |
| OK | Migration 051 indexes "not applied locally" — prod has them, local matters only for query plan tests | No action |
| Defer | mypy module resolution error (cross-import path conflict) | Not a CI blocker (CI passing); mypy run from inside subdirs works |
| Defer | Ruff lint nits (4 small style issues in scripts/tests) | Auto-fixable later; not regression |

## Files changed

| File | Change |
|------|--------|
| `website/frontend/src/api/types.ts` | New `CompositeStatsPlayer` + `CompositeStatsResponse` types |
| `website/frontend/src/api/client.ts` | New `getCompositeStats` API method |
| `website/frontend/src/api/hooks.ts` | New `useStoryComposite` React Query hook |
| `website/frontend/src/components/story/CompositeStatsPanel.tsx` | NEW — TIR/CI/KPI/SDS/CP fingerprint panel (5 metric tabs) |
| `website/frontend/src/pages/Story.tsx` | Wire `<CompositeStatsPanel>` below `<InvisibleValuePanel>` |
| `website/js/route-registry.js` | Export `SESSION_DETAIL_TABS` constant |
| `website/js/session-detail.js` | Import shared `SESSION_DETAIL_TABS` from route-registry |
| `tools/backfill_round_canonical_id.py` | Read `POSTGRES_*` env names (was `DB_*` only) |
| `docs/CLAUDE.md` | Version 1.5.0 → 1.13.1, last-updated → 2026-05-08 |

## Verification status

- `npx tsc --noEmit` (frontend): 0 errors
- `node --check` on touched .js files: 0 errors
- `python3 -c "ast.parse"` on touched .py files: 0 errors

End-to-end testing left for user (browser + bot reload).

## Open follow-ups (deferred — explicit user decision needed)

1. **Run `python3 tools/backfill_round_canonical_id.py --apply`** on local
   samba (and prod, after review). Idempotent. Closes the 1900-NULL gap.
2. **Apply migration 050** (`UNIQUE` constraint on `round_canonical_id`) —
   only after backfill returns 0 NULLs.
3. **Apply migration 051** locally if you want EXPLAIN-driven query plan
   testing on samba (prod already has it).
4. Discord storytelling commands (`!gravity`, `!et_rating`, …) — new
   feature work, not a fix.
5. KIS `is_objective_area` / `distance_multiplier` placeholders.

## Skipped intentionally

- Brand-blue color "regression" — not a real regression (team A color)
- Pytest collection error — not present in actual run
- Migration 051 local apply — local-only env detail, prod has indexes
- Auto-running the round_canonical_id backfill — user owns data writes
