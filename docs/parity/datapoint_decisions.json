{
  "_note": "Keys the ledger must not count as unread, each with the reason. Prefix form '<endpoint> <sub>.*' covers a sub-object. A decision is an allowance: it needs a reason a reviewer can check, and the test refuses one that names a key no fixture carries.",
  "/api/storytelling/formula class_weights.*": "iterated: Story.tsx passes the whole object to TermGroup / Object.entries",
  "/api/storytelling/formula distance_multipliers.*": "iterated: Story.tsx TermGroup / Object.entries",
  "/api/storytelling/formula multipliers.*": "iterated: Story.tsx TermGroup / Object.entries",
  "/api/storytelling/formula oksii_multipliers.*": "iterated: Story.tsx TermGroup / Object.entries",
  "/api/storytelling/formula validity.*": "iterated: Story.tsx TermGroup / Object.entries",
  "/api/rounds/{round_id}/awards categories.*": "iterated: RoundsTab.tsx Object.entries(awards.categories)",
  "/api/stats/session/{gaming_session_id}/good-night components.*": "iterated: SessionDetail.tsx Object.entries(data.components)",
  "/api/stats/session/{gaming_session_id}/detail scoring.debug": "debug block for operators, not a datapoint for a visitor",
  "/api/stats/last-session scoring.debug": "debug block for operators, not a datapoint for a visitor",
  "/api/stats/session/{gaming_session_id}/detail players.played_pct_lua": "retired on purpose: a literal copy of played_pct (sessions_router.py:2203-2204, BACKLOG 2026-09-03); alive_pct_lua and alive_pct_diff are NOT copies (engine TAB[8] vs computed) and stay unread",
  "/api/live/state server_time": "transport field (the server's clock for the poll), not a datapoint",
  "/api/datasets _note": "documentation string inside the payload",
  "/api/stats/last-session map_counts.*": "data-map: the keys are map names (a table of counts by map), not fields",
  "/api/storytelling/kill-impact entries.*": "duplicate: `entries` is the same list as `players` (kis.py serves both names for the legacy page); the Story page reads `players`",
  "/api/players/{identifier}/card percentiles.*": "iterated: PlayerCardSection prints every percentile through Object.entries, so the pool's keys are not named in the source",
  "/api/greatshot/{demo_id} highlights.clip_demo_path": "the server path of the cut clip; clip_download is built from the same row, so the path is the link's title and never a state of its own (Codex on #1003)",
  "/api/proximity/event/{event_id} round_end_unix": "the round's end wall time; the page prints the canonical round_duration_seconds instead of end − start, which includes pauses and reads 0:00 on zero sentinels (Codex on #1004)",
  "/api/planning/today session.created_at": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today session.created_by_user_id": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today session.discord_thread_id": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today session.my_vote_suggestion_id": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today session.suggestions": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today session.teams": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today session.updated_at": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today unlocked": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today viewer": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/planning/today viewer.website_user_id": "Tier 4, not built (2026-09-09): planning_sessions / planning_votes / planning_teams / planning_team_members hold 0 rows (measured 2026-09-09 and 2026-09-08); the old React planning panel always hit a 404. Read when the feature has a first row.",
  "/api/proximity/prox-scores players.rank": "assigned after the service filtered the cohort to this guid (useProxScoresForPlayer always sends player_guid), so it is 1 in every per-player answer; the cohort rank is the proximity leaderboard's (Codex on #1009)",
  "/api/proximity/summary v5_counts.*": "iterated: Object.entries prints every source table's row count under 'v5 source rows in this scope' (ProximityOutcomes)",
  "/api/proximity/events events.reaction_ms": "an alias: get_proximity_events sets reaction_ms and duration_ms from the same column (row[6]); combat_engagement has no reaction column, the drill-down's attackers carry first_hit_ms (Codex on #1010)",
  "/api/skill/formula weights.*": "iterated: FormulaWeights prints every metric's signed weight, its description (metrics.*) and its source (metric_sources.*) — SkillRating.tsx",
  "/api/skill/formula metrics.*": "iterated: printed beside each weight in FormulaWeights (SkillRating.tsx)",
  "/api/skill/formula metric_sources.*": "iterated: FormulaWeights maps every metric to its source (pcs / proximity) — SkillRating.tsx",
  "/api/stats/trends map_distribution.*": "iterated: Home's insights sort the whole map distribution and draw the top eight",
  "/api/greatshot/{demo_id}/crossref db_player_stats": "the per-player db rows keyed by nickname; comparison[].db_stats carries the same rows, already printed per player",
  "/api/stats/last-session unassigned_players.denied_playtime": "Home's last-night card is the box score and five sums; the same players' full 22 columns are the session page's basics table (SessionDetail), where these are read",
  "/api/stats/last-session unassigned_players.full_selfkills": "Home's last-night card is the box score and five sums; the same players' full 22 columns are the session page's basics table (SessionDetail), where these are read",
  "/api/stats/last-session unassigned_players.kill_assists": "Home's last-night card is the box score and five sums; the same players' full 22 columns are the session page's basics table (SessionDetail), where these are read",
  "/api/stats/last-session unassigned_players.self_kills": "Home's last-night card is the box score and five sums; the same players' full 22 columns are the session page's basics table (SessionDetail), where these are read",
  "/api/stats/last-session unassigned_players.time_dead_seconds": "Home's last-night card is the box score and five sums; the same players' full 22 columns are the session page's basics table (SessionDetail), where these are read",
  "/api/stats/player/{player_name} discord_linked": "usePlayerIdentity is read for the identity block only; its stats.* and discord flag duplicate the profile endpoint's lifetime/identity sections (PlayerProfile.tsx, the note at the identity panel) and are deliberately not shown twice",
  "/api/stats/player/{player_name} stats.favorite_map": "usePlayerIdentity is read for the identity block only; its stats.* and discord flag duplicate the profile endpoint's lifetime/identity sections (PlayerProfile.tsx, the note at the identity panel) and are deliberately not shown twice",
  "/api/stats/player/{player_name} stats.favorite_weapon": "usePlayerIdentity is read for the identity block only; its stats.* and discord flag duplicate the profile endpoint's lifetime/identity sections (PlayerProfile.tsx, the note at the identity panel) and are deliberately not shown twice",
  "/api/stats/player/{player_name} stats.games": "usePlayerIdentity is read for the identity block only; its stats.* and discord flag duplicate the profile endpoint's lifetime/identity sections (PlayerProfile.tsx, the note at the identity panel) and are deliberately not shown twice",
  "/api/stats/player/{player_name} stats.playtime_hours": "usePlayerIdentity is read for the identity block only; its stats.* and discord flag duplicate the profile endpoint's lifetime/identity sections (PlayerProfile.tsx, the note at the identity panel) and are deliberately not shown twice",
  "/api/stats/player/{player_name} stats.total_xp": "usePlayerIdentity is read for the identity block only; its stats.* and discord flag duplicate the profile endpoint's lifetime/identity sections (PlayerProfile.tsx, the note at the identity panel) and are deliberately not shown twice",
  "/api/stats/last-session map_counts": "counts ROUNDS per map (an ordinary map is 2, a replayed one 4); the Home card counts plays from the R1 rows of matches instead (Codex on #1011)",
  "/api/stats/session/{gaming_session_id}/detail players.alive_pct_lua": "the tracker's second Alive% measurement; the players table retired its Lua column on purpose (the page test pins no 'lua' header) and the drift is summarised per session from alive_pct_drift instead",
  "/api/stats/session/{gaming_session_id}/detail players.alive_pct_diff": "engine minus tracker Alive% — data about the capture, retired from the players table with the Lua column (see alive_pct_lua)",
  "/api/players/{identifier}/profile aim": "deferred by cost: the SPA never requests sections=aim/advanced (8–46 s cold, docs/research/PROFILE_AIM_ADVANCED_RCA_2026-09-08.md); the rows return when a producer warms the migration-077 cache — owner's path A–F",
  "/api/players/{identifier}/profile aim.*": "deferred by cost: the SPA never requests sections=aim/advanced (8–46 s cold, docs/research/PROFILE_AIM_ADVANCED_RCA_2026-09-08.md); the rows return when a producer warms the migration-077 cache — owner's path A–F",
  "/api/players/{identifier}/profile advanced": "deferred by cost: the SPA never requests sections=aim/advanced (8–46 s cold, docs/research/PROFILE_AIM_ADVANCED_RCA_2026-09-08.md); the rows return when a producer warms the migration-077 cache — owner's path A–F",
  "/api/players/{identifier}/profile advanced.*": "deferred by cost: the SPA never requests sections=aim/advanced (8–46 s cold, docs/research/PROFILE_AIM_ADVANCED_RCA_2026-09-08.md); the rows return when a producer warms the migration-077 cache — owner's path A–F",
  "/api/availability/promotion-preferences encryption_enabled": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/availability/promotion-preferences notify_threshold": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/availability/promotion-preferences quiet_hours": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/availability/promotion-preferences signal_handle": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/availability/promotion-preferences signal_handle_masked": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/availability/promotion-preferences telegram_handle": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/availability/promotion-preferences telegram_handle_masked": "not built: telegram/signal/encryption/quiet-hours/threshold preferences — 2 website users, owner tier 4 (audit 2026-09-07 B1); the SPA's availability page shows the Discord-side preference only",
  "/api/skill/movers form_weights.*": "iterated: Home.tsx prints every weight through Object.entries",
  "/api/skill/player/{identifier}/form form_weights.*": "iterated: PlayerProfile.tsx prints every weight through Object.entries",
  "/api/awards filters": "request echo: the server repeats the player/award_type/days the page itself chose (Awards.tsx state); printing the reader's own selection back is not information",
  "/api/awards/leaderboard filters": "request echo: the server repeats the player/award_type/days the page itself chose (Awards.tsx state); printing the reader's own selection back is not information",
  "/api/stats/session/{gaming_session_id}/basics players.denied_playtime_seconds": "the raw seconds behind denied_pct, which IS the basics column (owner's 17-column list, 2026-09-03; SessionDetail.test pins the count) — a second denied column would break the list for a number the ratio already carries",
  "/api/proximity/prox-scores/formula category_weights": "duplicate: the same three weights as categories[].weight_in_overall, which the page prints (fixture: 0.4/0.35/0.25 on both)",
  "/api/proximity/player/{guid}/radar unscored.avg_dodge_reaction_ms": "duplicate: the same AVG(r.dodge_reaction_ms) the movement endpoint returns as avg_dodge_ms, which the page already shows as the 'dodge' tile (proximity_movement.py:219/241; both read 1,431 ms in the recording)",
  "/api/stats/records revived": "deliberate omission carried from legacy (RecordBook.tsx header): the backend sends a 'times revived' record, the legacy site never rendered it; adopting it is an owner decision",
  "/api/records/maps/segments records.fastest_seconds": "alias: fastest_time is the m:ss of fastest_seconds and is the column shown (fixture: 205 → '3:25')",
  "/api/records/maps/segments records.winner_team": "alias: winner_side is the server's string for winner_team and is the column shown (fixture: 1 → 'Axis')",
  "/api/stats/last-session unassigned_players.time_dead_seconds_raw": "the pre-repair value behind time_dead_seconds (time-fields lane 2026-09-02); the page shows the repaired figure and the repaired flag, never the raw one",
  "/api/stats/weapons/by-player player_count": "PlayerDrilldown asks this endpoint for ONE guid, so player_count is always 1 there; the period-wide count is rendered on WeaponsPage from /by_player",
  "/api/availability statuses": "the status vocabulary the form's own options already enumerate (AvailabilityPage STATUS options); printing the enum would list the buttons twice",
  "/api/availability/access website_user_id": "an internal id the page never shows — the viewer is named by display name and linked guid; ids only travel in requests",
  "/api/proximity/objective-focus players.total_time_ms": "duplicate: the same figure as total_time_s, which the row prints (fixture: 810400 ms = 810.4 s)",
  "/api/proximity/competitive/man-advantage teams.ALLIES": "iterated: ProximityCompetitive prints every team through Object.entries(d.teams)",
  "/api/proximity/competitive/man-advantage teams.AXIS": "iterated: ProximityCompetitive prints every team through Object.entries(d.teams)"
}
