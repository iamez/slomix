# W1 — triage of historical 500s (2026-07-29)

Task W1 from a session working-doc (`docs/TASKS_FOR_SONNET_2026-07-29.md` — not tracked in
this repo, so that filename is not a citable source). `logs/access.log` and `logs/errors.log`
are also **not tracked** (`.gitignore` excludes `*.log`/`logs/`) — the specific occurrence
counts and dates below were pulled from this box's live log files and aren't independently
reproducible from a fresh checkout; treat them as this box's measurement at the time of
writing, the same way the rest of this backlog treats `crontab -l`/`ps` output. What *is*
independently verifiable from tracked code: the root-cause code paths cited (session_scope.py,
logging_config.py, security_utils.py) and the live replay results against a running backend.
Method: pull every `→ 5xx` line from `logs/access.log`, find the matching request/traceback,
then replay live against the local dev backend
(`website/venv/bin/uvicorn backend.main:app`, already running on `127.0.0.1:8000`).

## Structural limitation found first

`routed_path()` (`website/backend/security_utils.py:29`) — used by the logging middleware
for every `→ METHOD /path` and `← METHOD /path → status` line via `safe_path` — returns
**only** `request.scope["path"]`, never the query string. So for any endpoint that takes
query parameters, `access.log` cannot tell you what parameters produced a given 500.

This is not "query strings are never logged anywhere," though — `_check_security()`
(`logging_middleware.py:275`) separately reads `request.scope["query_string"]` and attaches
it (truncated to 200 chars) as an `extra={"query": ...}` field on a `security_logger.warning`
call, but **only** when the query matches one of `SUSPICIOUS_PATTERNS` (SQL-injection-style
strings etc.) — none of the failures below are that. And even for a request that did match,
the query wouldn't actually appear in `security.log`'s rendered text **regardless of
formatter**: the active formatter is `StandardFormatter` (`logging_config.py`), whose format
string has no `%(query)s` placeholder — but the JSON formatter doesn't cover it either.
`JSONFormatter.format()` (`logging_config.py:137-157`) copies a fixed allowlist of `extra`
keys (`request_id`, `user_id`, `client_ip`, `method`, `path`, `status_code`, `duration_ms`) and
`query` is not one of them, so enabling `LOG_FORMAT_JSON` wouldn't surface it either — this
isn't a "wrong formatter is active" gap, the field is dropped either way. So in practice, for
the ordinary (non-attack-pattern) 500s this doc investigates, the query string genuinely isn't
recoverable from any log on this box today — the security-logging path just isn't the reason,
and doesn't contradict that conclusion.

## Per-endpoint findings

| Endpoint | Occurrences in access.log | Root cause | Reproduces now? |
|---|---|---|---|
| `/api/proximity/prox-scores` | 7× (2026-07-19 → 2026-07-20 only — re-grepped against the live `logs/access.log` for this fix; an earlier pass of this doc had claimed 8× spanning back to 2026-04-03, which the current file doesn't contain evidence for) | **Identified** — see below, and now entirely within the diagnosed cause's window (see next section) | No — the incident report's exact URL (`?session_date=2026-07-18`) replays **200 on a verified cache MISS**, and the "no" is backed by a diagnosed cause rather than a failed repro attempt |
| `/api/proximity/player/{guid}/radar` | 1× (2026-07-19, guid `FB0EC84076637A9F55D579085A3225C4`) | **Identified** — same cause as prox-scores | No, same reasoning |
| `/api/predictions/recent` | 5× (2026-07-20 → 2026-07-24) | Not identified — no traceback survives rotation | No. 200 with no params. |
| `/api/proximity/leaderboards` | 4× (2026-07-27, same day errors.log starts) | Identified and fixed, #564 | No — confirmed fixed. |
| `/api/storytelling/win-contribution` | 3× (2026-04-19) | Not identified — no traceback survives rotation | No, tested with a **real resolvable session** (see below), not a placeholder date. |
| `/api/diagnostics/storytelling-completeness` | 6× (2026-05-05) | Not identified | No. 200 with `session_date=2026-07-27`, and also 200 with `2026-07-20` — this endpoint doesn't use `resolve_story_scope()`, so it runs its queries for any valid date (no cache involved either; see below). |
| `/api/seasons/current/awards` | 1× (2026-06-13) | Not identified | No. 200 with no params. |

## `prox-scores` and `player radar`: root cause identified, not just "not reproducing"

`docs/research/SMART_STATS_SESSION_SCOPE_REVIEW_AND_REMEDIATION_PLAN_2026-07-19.md` (§16.2-16.3,
§17.4) already diagnosed both of these from a live 2026-07-19 investigation — found on a second
pass through this doc after a reviewer pointed out the citation existed. Summary: the dev
`etlegacy-web` process had stayed alive since 2026-07-07 22:17 with zero restarts, so its
in-memory Python route handlers were still the July-7 version, while `StaticFiles` (which reads
the frontend off the mutable checkout on every request) served much newer JS, and some
request-time lazy imports pulled newer Python modules into the same old process. Concretely:

- **prox-scores**: the old handler expected `compute_prox_scores()` to return a `list` and ran
  `results[:limit]` on it; a lazy import pulled in the *new* v2 service, which returns a
  `dict` — `TypeError: unhashable type: 'slice'`, logged as `prox-scores failed`, matching this
  doc's original grep exactly.
- **player radar**: same mixed-revision cause, inverse symptom — the long-lived old route
  function indexed `prox_scores[0]` (list access) against the new dict result —
  `KeyError: 0`.
- Both docs are explicit that **current tracked source is already correct**:
  `website/backend/routers/proximity_player.py:178-204` already consumes
  `prox_result.get("players", [])`, not list indexing. "The issue is the live process, not a
  request to re-implement that current fix."

So: not reproducing today is expected and explained, not merely unconfirmed. The mechanism was
a stale long-running dev process mixing code revisions — a deploy-hygiene issue distinct from
the endpoint logic itself, and the historical prod side of that same investigation (§16.3
table) shows production stayed internally coherent throughout and never 500'd on either
endpoint for the same requests.

This attribution only covers occurrences from 2026-07-19 onward (the process had been alive
since 2026-07-07, but the actual failing requests are 07-19/07-20) — it would **not** explain
any occurrence that predates the process's 07-07 start. An earlier pass of this doc claimed a
wider range going back to 2026-04-03, which doesn't hold up: re-grepping the live
`logs/access.log` for this fix found only the 7 occurrences above, all on 07-19/07-20, nothing
in April-June. If that earlier count came from a log file that's since rotated away, it can't
be re-verified either way — but nothing currently on this box supports attributing pre-07-07
prox-scores failures to this cause, so the table above no longer claims one.

## `win-contribution`: tested against a real session, not a placeholder

This caveat applies to **`win-contribution` only** — an earlier pass of this doc grouped
`storytelling-completeness` under it, which was wrong. That endpoint
(`diagnostics_router.py:655`) doesn't call `resolve_story_scope()` at all: it parses any
syntactically valid date and runs its `fetch_one()` queries directly, including for dates with
no gaming session. So its original `session_date=2026-07-20` replay *did* exercise the handler
and its 200 was a valid result, not a scope-resolution short-circuit. Re-confirmed live:
`/api/diagnostics/storytelling-completeness?session_date=2026-07-20` → **200**, and with no
`X-Cache` header at all, so the handler genuinely ran (Codex review on #573).

`win-contribution` is the one that short-circuits: with `session_date=2026-07-20` (no matching
gaming session locally) `resolve_story_scope()`
(`website/backend/services/session_scope.py:296-302`) 404s before the handler runs any of the
queries that historically failed — re-confirmed live, that URL still returns **404** today — so
for *that* endpoint the placeholder test proved nothing. Re-tested with a real session pulled
from the database:

```bash
$ PGPASSWORD=... psql -h 127.0.0.1 -U etlegacy_user -d etlegacy -t \
    -c "SELECT gaming_session_id, round_date FROM rounds WHERE gaming_session_id IS NOT NULL ORDER BY round_date DESC LIMIT 3;"
   139 | 2026-07-27

$ curl -s -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:8000/api/storytelling/win-contribution?session_date=2026-07-27'
200
$ curl -s -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:8000/api/diagnostics/storytelling-completeness?session_date=2026-07-27'
200
```

Both 200 against a session that actually exists and actually exercises the query path. Still
not proof of a fix (no traceback survived rotation for either), but a materially stronger "not
reproducing" than the original placeholder-date test.

## Why "cannot reproduce" is still not "confirmed fixed" for the rest

`predictions/recent`, `win-contribution`, `storytelling-completeness`, and
`seasons/current/awards` have no surviving traceback (errors.log only retains back to
2026-07-27, past every occurrence) and no diagnosed root cause the way prox-scores/radar now
have. Per the task's own rule ("if you cannot reproduce, say so explicitly — do not record it
as fixed"): **not confirmed fixed, only not-currently-reproducing** for these four.
`leaderboards` has a specific attributed fix (#564). `prox-scores`/`radar` now have an
identified, documented cause (dev-process staleness) that is distinct from "unconfirmed."

## Recommendation

If a 500 recurs on any of these endpoints, the fastest way to get real parameters is the
client-side error reporting work in W4 (`POST /api/client-error` with **the failed request's
own URL**, not just the page URL — see that PR's fix for why page URL alone doesn't capture a
separate API fetch's query string). This doc doesn't change `routed_path()` itself: that
function is deliberately security-scoped (Host-header spoofing prevention) and logging query
strings server-side risks leaking session tokens or GUIDs into a file with looser retention
than intended; better to close the loop from the client side where the page already knows what
it asked for.

## Verify

**Show `X-Cache` — a status-only curl can't tell a live replay from a cached one.**
`HTTPCacheMiddleware` caches the `/api/proximity/`, `/api/storytelling/` and
`/api/seasons/current` prefixes and can return a stored 200 *before* dispatching to the route,
so `-o /dev/null` (which also hides the headers) would let a cached success masquerade as proof
that current code handles the request. Every replay below must show `X-Cache: MISS` (or no
`X-Cache` header, meaning the path isn't cached at all) to count (Codex review on #573):

```bash
probe() {
  local hdr code xc
  hdr=$(curl -sS -D- -o /dev/null --max-time 20 "$1")
  code=$(printf '%s' "$hdr" | head -1 | awk '{print $2}')
  # Assign first, then default with ${xc:-none}. A `... | awk ... || echo none`
  # pipeline never runs the fallback: awk exits 0 even when grep found nothing,
  # so the whole pipeline succeeds and prints an empty string — the function
  # reported a bare "X-Cache=" and could not distinguish "uncached path" from
  # "header missing" (Codex review on #573).
  xc=$(printf '%s' "$hdr" | grep -i '^x-cache:' | tr -d '\r' | awk '{print $2}')
  printf '%-72s -> %s  X-Cache=%s\n' "${1#http://127.0.0.1:8000}" "$code" "${xc:-none}"
}

# The one request with surviving forensic evidence (the incident report's exact URL —
# get_prox_scores() parses session_date and takes the service's distinct exact-scope path,
# which none of the rolling-window variants below exercise):
probe 'http://127.0.0.1:8000/api/proximity/prox-scores?session_date=2026-07-18'

# no-param / fixed-param endpoints
probe 'http://127.0.0.1:8000/api/proximity/prox-scores'
probe 'http://127.0.0.1:8000/api/proximity/player/FB0EC84076637A9F55D579085A3225C4/radar'
probe 'http://127.0.0.1:8000/api/predictions/recent'
probe 'http://127.0.0.1:8000/api/proximity/leaderboards'
probe 'http://127.0.0.1:8000/api/seasons/current/awards'

# prox-scores rolling-window variants
probe 'http://127.0.0.1:8000/api/proximity/prox-scores?range_days=7'
probe 'http://127.0.0.1:8000/api/proximity/prox-scores?limit=100'
probe 'http://127.0.0.1:8000/api/proximity/prox-scores?map_name=oasis'

# session-scoped endpoints, real session (2026-07-27 as of this writing).
# If that's aged out, don't just take MAX(round_date): resolve_story_scope() ->
# _find_gsids_for_date() applies the _ROUND_GATE_SQL gate, AND returns every session on the
# date — a date shared by two gaming sessions makes resolve_gaming_session_scope() answer 409
# rather than exercising the endpoint. Pick a date that is both gated-valid and unambiguous:
#   SELECT SUBSTR(CAST(round_date AS TEXT), 1, 10) AS d FROM rounds
#   WHERE gaming_session_id IS NOT NULL AND round_number IN (1, 2)
#     AND is_valid IS DISTINCT FROM FALSE
#     AND (round_status IN ('completed', 'substitution') OR round_status IS NULL)
#   GROUP BY 1 HAVING COUNT(DISTINCT gaming_session_id) = 1
#   ORDER BY d DESC LIMIT 1;
probe 'http://127.0.0.1:8000/api/storytelling/win-contribution?session_date=2026-07-27'
probe 'http://127.0.0.1:8000/api/diagnostics/storytelling-completeness?session_date=2026-07-27'
```

Measured 2026-07-30 with the cache visible — the two that matter most:

```
/api/proximity/prox-scores?session_date=2026-07-18                  -> 200  X-Cache=MISS
/api/diagnostics/storytelling-completeness?session_date=2026-07-20   -> 200  X-Cache=none
```

The first is the diagnosed request replaying clean on a genuine cache miss (stronger evidence
than this doc previously had); the second is the placeholder-date call that this doc had wrongly
written off — no cache involved, the handler ran.
