# Website Backend Dependencies (synced with root requirements.txt)
fastapi==0.133.1
uvicorn[standard]==0.41.0
python-dotenv==1.2.2
asyncpg==0.31.0
python-multipart==0.0.31
httpx==0.28.1
itsdangerous==2.2.0
slowapi==0.1.9
# Observability (IMP-005): prod runs the web service from venv-web built off
# THIS file — without these pins the /metrics endpoint and the Redis cache
# backend silently degrade to no-ops. Versions match the root requirements.txt.
prometheus-client==0.24.1
prometheus-fastapi-instrumentator==7.1.0
redis==7.2.1
# Explicit starlette pin (matches the root manifest and both prod venvs):
# the instrumentator constrains starlette<1.0, so without a pin the resolver
# floats inside the 0.x line and CI audits a nondeterministic version. The
# 0.x advisories are the same time-boxed, owner-accepted debt as in the root
# requirements (see the pip-audit ignores in .github/workflows/tests.yml;
# they drop with the FastAPI/starlette 1.x migration).
starlette==0.52.1
